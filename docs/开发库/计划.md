# CoBeing 开发计划（开发库）

> 本文件追踪「有什么开发计划」。最新在最上；权威待办与决策记录见 `../项目信息/当前待办.md`。
> **本文件是计划视图，只列「意向」，不做逐条变更日志；落地后在 `功能清单.md` 对应项标 ✅，并把状态回填到 `当前待办.md`。**

---

## 近期进行中（红线区）

- [ ] **提交 08-12「真实软件接入 v1」未提交工作**（git commit：浏览器 MCP + QQ 接线 + 相关测试与文档）
- [ ] **产出 v1.4.0 发布包**（releases/ 当前最新 v1.3.1；补全 `PROGRESS-VERSION.md` v1.4.0 尾号 + 打包 zip）
- [ ] **管家人格切换言行真实回归**（4 人格回执/转接体验，配真实对话）
- [ ] **对话式首启收集效果真实用户验证**（初始 Agent 贴合度、管家喜好理解）

## 待办（引用 `当前待办.md`，保持同步）

### P0
- [ ] 旧 `__cobeing*` 全局变量与 Runtime namespace 收敛（统一访问器）
- [ ] Market certified 审核流程 / 认证远程市场 / 社区资源版本更新机制
- [ ] 群组 → Butler 结构化事件桥的交互式渲染（回执/等待用户决策卡）

### P1
- [ ] ToolAgent 输出统一评估 / 降级框架
- [ ] TODOboard P1 完整协议（统一 TriggerEngine / cron 追赶）

### P2
- [ ] 长会话内存增长观测、上下文体积监控与告警
- [ ] 沙箱镜像构建（Docker Hub 网络不稳定，建议配置国内 registry mirror 后重跑 `scripts/build-sandbox.sh`）
- [ ] 普通用户生活场景闭环（旅行 / 学习 / 家庭 / 创作 / 个人研究）

> 完整清单与决策表（12 条，全 ✅）见 `../项目信息/当前待办.md`。计划项由该文档派生，二者若不一致以 `当前待办.md` 为准。

---

## 方向性计划（阶段性判断）

- **短期**：收尾 v1.4.0（提交 + 打包 + 真实软件接入补强），继续验证 QQ/浏览器真实链路。
- **中期**：Market 分级完整化（certified 审核、远程市场、persona/plugin 资源入安装链路）；管家入口作为唯一默认入口的闭环打磨。
- **长期**：用「真实软件接入」证明 CoBeing 能减少用户杂事 → 生活场景闭环 → 朝「普通人超级个体系统」演进（愿景见 `../GOAL.md`、`../项目信息/产品战略.md`）。

---

## 本库维护约定

- 计划项来自 `当前待办.md` 与产品/技术方向；新想法 → 先入 `想法.md`，成熟后升为「计划」再入 `计划.md`。
- 计划是否「进行中」「已完成」以 `功能清单.md` 标记与 `当前待办.md` 状态为准。
