# CoBeing 测试清单

> 更新日期: 2026-05-25
> 共 43 个测试文件, 417 tests, 全部通过

---

## 工具系统 (41 tests)

### `tools/bash.test.ts` (4 tests)
- executes a simple command
- returns error for failed command
- truncates output exceeding 16384 bytes
- does not truncate short output

### `tools/edit-file.test.ts` (8 tests) — 新增
- replaces a single occurrence of old_string
- replace_all replaces all occurrences
- rejects when old_string equals new_string
- returns error when old_string not found
- rejects when old_string appears multiple times without replace_all
- includes old/new preview in output
- truncates old/new preview at 80 characters
- returns error when file does not exist

---

## Agent 相关 (27 tests)

### `agent/paths.test.ts` (15 tests)
- AgentPaths resolves paths from base dir
- AgentPaths resolves new paths (user, bootstrap, tools)
- AgentPaths ensures dirs exist
- AgentFiles writes and reads IDENTITY.md
- AgentFiles returns empty identity for missing file
- AgentFiles writes and reads SOUL.md
- AgentFiles writes and reads config.json
- AgentFiles returns empty config for missing file
- AgentFiles writes and reads USER.md
- AgentFiles returns empty string for missing USER.md
- AgentFiles writes and reads BOOTSTRAP.md
- AgentFiles returns empty string for missing BOOTSTRAP.md
- AgentFiles writes and reads TOOLS.md
- AgentFiles deletes BOOTSTRAP.md after consume
- AgentFiles returns empty for consumeBootstrap when no file

### `agent/registry.test.ts` (4 tests)
- registers an agent
- gets an agent by id
- unregisters an agent
- lists all agents

### `agent/event-bus.test.ts` (6 tests)
- subscribes and receives events
- unsubscribes correctly
- multiple subscribers receive events
- agent does not receive own events
- filtered subscription by type
- task-complete event type

### `agent/butler.test.ts` (2 tests)
- creates butler with management tools
- butler has skill repository injected

---

## 群组相关 (43 tests)

### `group/context.test.ts` (16 tests)
- speakToMain appends message
- onMainMessage fires listener
- getMainHistory returns all messages
- isMentioned detects @agent-id
- isMentioned detects @all
- getPendingMentions filters for agent
- createTalk creates with members and topic
- Talk.speak appends message
- Talk.getHistory returns messages
- Talk.isMember checks membership
- saveMain writes to file
- saveTalk writes talk to file
- saveConfig writes config
- speakToMain triggers event bus
- multiple mentions in one message
- getPendingMentions with sinceIndex

### `group/protocol.test.ts` (7 tests)
- RoundRobinProtocol assigns turns
- RoundRobinProtocol cycles members
- FreeFormProtocol returns all members
- ModeratedProtocol returns owner first
- ModeratedProtocol includes others after owner
- createProtocol returns correct type
- createProtocol throws for unknown type

### `group/manager.test.ts` (3 tests)
- creates and retrieves group
- lists all groups
- deletes group

### `group/roles.test.ts` (8 tests)
- ChannelBindTo user role
- ChannelBindTo owner role
- ChannelBindTo agent type
- BindingEntry defaults
- GroupConfig with owner
- GroupConfig without protocol
- GroupMessage metadata
- AgentStatusInfo

### `group/router.test.ts` (9 tests)
- routes to butler when no bindTo configured
- injects message to group main channel (user mode)
- returns recent main history as response (user mode)
- creates persistent talk and injects message (owner mode)
- reuses same talk on subsequent messages (owner mode)
- unbind restores default butler routing
- unbind owner mode cleans up talk reference
- loads bindings from config
- allows updating butler callback

---

## 工具相关 (16 tests)

### `tools/registry.test.ts` (4 tests)
- registers a tool
- gets tool by name
- lists tool definitions
- overwrites duplicate tool

### `tools/permission.test.ts` (6 tests)
- full-access allows all
- workspace-readwrite allows write in workspace (L1)
- workspace-access + basic-access permission checks
- read-only denies write
- ask mode prompts
- custom allow list
- custom deny list

### `tools/executor.test.ts` (6 tests)
- executes registered tool
- denies unregistered tool
- enforces permissions
- passes tool context
- handles tool errors
- passes through tool execution errors

---

## 记忆与经验 (37 tests)

### `memory/sqlite-adapter.test.ts` (22 tests)
- inserts and reads entries
- replaces all entries for a target
- updates an entry by id
- deletes an entry by id
- finds entry by substring
- counts chars for a target
- persists data to disk
- searches entries across targets
- filters search by target
- returns empty for no match
- inserts and searches history
- filters history by session
- stores and retrieves sync mtime
- returns results with scoring fields for matching query
- scores exact matches higher than unrelated entries
- has temporal_decay scores in valid range
- returns empty array for no matches (multi-strategy)
- respects limit parameter
- adjusts trust score up for helpful
- adjusts trust score down for unhelpful
- clamps trust to 0 at minimum
- increments helpful/unhelpful counters

### `memory/hrr.test.ts` (6 tests)
- has 1024 dimensions
- encodeAtom throws 'not implemented'
- bind throws 'not implemented'
- unbind throws 'not implemented'
- bundle throws 'not implemented'
- similarity throws 'not implemented'

### `memory/writer.test.ts` (4 tests)
- appends memory entry
- creates daily file
- searches memory
- handles concurrent writes

### `memory/experience.test.ts` (6 tests)
- appends experience entry
- reflects and extracts experience
- searches experiences
- skips short reflections
- skips entries with "无"
- loads prompt template from file

### `butler/registry.test.ts` (10 tests)
- registers agent entry
- unregisters agent
- gets agent by id
- parseAgentsRegistry reads file
- registers group entry
- unregisters group
- parseGroupsRegistry reads file
- appends task log
- reads task log
- gets agent groups

---

## 技能相关 (7 tests)

### `skills/md-loader.test.ts` (7 tests)
- loads SKILL.md from directory
- loads SKILL.md from file
- skips invalid SKILL.md
- handles missing frontmatter
- extracts parameters
- generates tool definition
- filters skills by name

---

## Prompt 相关 (9 tests)

### `conversation/prompt-builder.test.ts` (9 tests)
- builds prompt from role when no files exist
- prepends SOUL.md at the very top
- includes BOOTSTRAP.md and deletes the file
- appends USER.md preferences
- appends AGENTS.md workspace guide
- appends EXPERIENCE.md when non-trivial (>50 chars)
- skips short EXPERIENCE.md (noise)
- appends MEMORY.md index
- full chain order is correct

---

## 工作流 (3 tests)

### `workflow/engine.test.ts` (3 tests)
- analyzes task and returns suggestions
- generates execution plan
- handles analysis failure

---

## Provider (6 tests)

### `providers/catalogs/catalogs.test.ts` (6 tests)
- Anthropic catalog valid
- OpenAI catalog valid
- DeepSeek catalog valid
- Gemini catalog valid
- 智谱 catalog valid
- 通义 catalog valid

---

## 共享 (3 tests)

### `shared/events.test.ts` (3 tests)
- EventEmitter on/off
- EventEmitter once
- EventEmitter multiple listeners

---

## 集成测试 (18 tests)

### `integration.test.ts` (18 tests)
- Agent 创建时自动建立目录结构
- 从 IDENTITY.md 和 SOUL.md 加载增强 system prompt
- 对话自动写入记忆文件
- 群组 main 频道 @mention 路由正确 (GroupContextV2)
- talk 私有讨论隔离 (GroupContextV2)
- 群组 + GroupManager + Registry 协同 (v2)
- Agent 创建→注册→销毁 完整生命周期 (ButlerRegistry)
- 任务日志写入和读取
- Group 注册表与 Agent 关联
- SkillRepository loads and Agent injects skill tools
- 搜索历史对话关键词 (MemoryReader)
- Agent 完成任务后自动创建 EXPERIENCE.md
- GroupContextV2 @mention 解析正确
- skill-list respects skills whitelist
- SkillRepository create and search
- bootstrap is consumed after agent creation (Phase 8.1)
- agent prompt includes SOUL + USER + AGENTS (Phase 8.1)
- AgentPaths resolves all new paths (Phase 8.1)
