# CoBeing 当前待办

> 最后更新：2026-08-09  
> 本文档只保留当前仍然有意义的待办，不再维护旧版全量路线图。

---

## 2026-08-08/09 第一性原理报告决策记录（12 条）

来源：`docs/information/2026-08-08-research-cobeing-first-principles.md` 提交后用户逐条裁决。状态图例：✅已执行 / 📌待办 / 🔬需调研。

| # | 决策 | 状态 |
|---|---|---|
| 1 | **群主约束依靠 prompt，但 prompt 需强硬约束**：不撤执行工具；强化 HOST_JOB.md/管家 JOB.md 约束措辞，陈默场景复验 | ✅ 措辞强化（HOST_JOB.md 硬措辞 + 4 人格 JOB.md 执行工具说明）；**管家工具分级**（P2）2026-08-09 硬约束实施：BUTLER_DEFAULT_TOOLS 移除 bash/edit-file/glob/grep，运行时强制过滤 + 修复 config.json，冒烟 19/19 |
| 2 | **群组多智能体并发是核心功能**：工程防护（并发写防覆写） | ✅ 2026-08-09 并发写防护实施：file-version CAS（read-file 附版本行，write/edit-file baseVersion 校验拒绝过期覆盖）+ 7 测试 |
| 3 | **TODOboard 本质是隔离的内容**：需改进方案 | ✅ 2026-08-09 P0 改进实施：repeat/nextTriggerAt/overduePolicy 字段 + getRepeatDueTodos/advanceRepeat + stale 重唤醒 + Agent 级 0time/repeat/condition 触发 + todo-add 支持 + GUI 标记（+11 测试）。P1 完整协议（统一 TriggerEngine/升级阶梯/cron 追赶）仍可后续 |
| 4 | **HRR 需要真实实现**（Phase 2：SHA-256 确定性相位向量） | ✅ 2026-08-09 实施：hrr.ts 真实现（encodeAtom/bind/unbind/bundle/similarity）+ 6 测试 |
| 5 | **群组协议固化已抛弃** | ✅ 文档修正（核心技术.md 更新） |
| 6 | **记忆纪律**：TTL/写入过滤/整合/并发写防护 | ✅ 2026-08-09 实施：MemoryEntry ttl(P0/P1/P2)/provenance 归一化 + Q1/Q2/Q3 写入纪律 + 每周整合 JOB.md + 写 CAS 共用（+2 测试） |
| 7 | **完全删掉投票** | ✅ 已执行（2026-08-09） |
| 8 | **工具智能体（ToolAgent）待确认** | ✅ 2026-08-09 实施：2 真主体 + 6 引擎确认 + 轻量 ToolAgentRegistry（复活 registerToolAgent 死注册 + loadAll）+ creator 双份 prompt 合并（配置卡权威，去 legacy character） |
| 9 | **experience-reflect 完全删除** | ✅ 已执行（2026-08-09） |
| 10 | **抛弃 Docker 沙箱，采用安全分类器** | ✅ 2026-08-09 实施：PermissionMode "auto" + SafetyClassifier（reasoning-blind/fail-closed/熔断/allow 缓存）+ PermissionEnforcer auto 决策链 + executor 接线 + tool:classified 审计（+14 测试）。Docker 沙箱保留为既有降级路径 |
| 11 | **GUI 只对外暴露管家**：需额外研究 | ✅ 2026-08-09 实施：NavBar 折叠「⋯」菜单（常驻管家/设置）+ advancedNav 开关（localStorage）+ 设置开关 + 教程文案 + 快捷键稳定 |
| 12 | **LLMGateway 问题需解释** | ✅ 已实现（2026-08-09 全局走 gateway，598 tests） |

---

## P0：文档与事实口径收敛

- 保持 `产品战略.md`、`项目现状.md`、`架构说明.md`、`使用说明.md`、`当前待办.md` 五份文档作为当前核心文档。
- 删除旧版能力清单式文档后，不再恢复这类容易过期的拆分文档。
- 后续新增能力必须在文档中区分“已实现、部分实现、计划中”。
- 入口文档不得恢复旧版冗余 Agent 文件名或旧版能力清单式口径。

---

## P0：旧全局变量与 Runtime namespace 收敛

代码中仍有若干旧式全局变量依赖，需要和当前 Runtime namespace 统一。

重点检查：

- `__cobeingAgentRegistry`
- `__cobeingHookBus`
- `__cobeingPromptLayers`
- `__cobeingRuntime`
- 其他 `__cobeing*` 兼容路径

（`__cobeingVoteStore` 已随投票功能于 2026-08-09 删除；运行时兼容别名已由 2026-08-01 骨架重构补齐）

这些问题会影响 MCP discover/register、plugin hook、prompt layer 等能力的真实可用性。

---

## P0：插件与 MCP 端到端验证

需要为以下链路做实际验证和测试：

- Provider 插件加载、启用、禁用、配置更新
- Channel 插件加载
- UI Extension 注册和前端展示
- HookBus notify/intercept/transform 是否在真实 Agent 流程中触发
- PromptLayer 是否进入 PromptBuilder
- MCP server 配置、连接、工具发现、工具注册、Agent 调用
- QQBot/Office MCP server 是否能在默认开发环境中可用

目标不是增加更多插件，而是证明当前扩展机制的主链路可靠。

**2026-08-09 完成**：新增 `scripts/verify-extensions.ts` 端到端验证 8/8 全过——HookBus notify/intercept/transform 三语义、PromptLayerRegistry 排序注入（含 provenance 标记）、UIExtensionRegistry 注册/按类型、PluginLoader 从 data/plugins/registry.json 加载 4 条、MCPManager 连接失败降级不崩溃。

---

## P0：Butler 托管闭环后续

2026-06-10 已完成第一轮后端闭环：Butler tracked dispatch 可创建 Global TODO、ButlerTask、Agent inbox / Group TODO；Agent task 状态可同步回 Global TODO / ButlerTask；新 Agent 默认生成 capability.json；WS `find_agent` / `dispatch_task` 已从占位改为真实操作；群组 prompt 已隔离私有 memory/experience。

仍需继续做：

- 补齐 Group -> Butler 的结构化 Host 事件工具：blocked、needs_user_decision、completed、failed、scope_change、status_digest。
- 让 Butler 入口的任务回执卡片、等待用户决策卡和完成结果卡接真实 ButlerTask / Global TODO 事件。
- 将 `agent-request-resource` 写入 Butler 审批/授权队列，而不是只返回文本。
- 继续为 Group -> Butler 结构化事件桥补可测单元；SQLite native binding 已通过 `better-sqlite3@12.10.0` 在 Node 24 下恢复，fallback 仅作为异常兜底。
- 明确取消、返工、等待用户后的恢复路径，避免只更新状态、不唤醒责任方。

**2026-08-09 已推进**：新增 `host-report-event` 工具（Group→Butler 结构化事件桥：blocked/needs_user_decision/completed/failed/scope_change/status_digest 六类 → 构造 ButlerEscalationEvent 广播 `butler_escalation` + 关联 ButlerTask，+3 测试）；`agent-request-resource` 不再只返回文本——广播 `butler_resource_request` + 追加管家工作区 RESOURCE_REQUESTS.md 收件箱。仍待跟进：前端回执卡片/等待用户决策卡接真实事件（后端事件已广播，前端 ws-handler 可接）。

---

## P1：ToolAgent 标准化后续收敛

2026-06-10 已补齐 `ToolAgentSpec` loader、Creator Group 创建草案、Memory 的 `MEMORY.md` 修改建议输出，并把 Memory prompt 对齐到当前五文件体系。

仍需继续做：

- 统一 ToolAgent 注册/发现机制，而不只是各调用点手动 import。
- 前端和日志中区分普通 Agent 工作与 ToolAgent 后台辅助工作。
- 为 ToolAgent 输出增加统一评估、降级和可审查记录。
- 明确哪些 Memory 修改建议可低风险自动应用，哪些必须交给用户或调用方确认。

**2026-08-09 已推进**：轻量 `ToolAgentRegistry`（register/getSpec/listSpecs/registerPluginAgent/loadAll）统一注册/发现入口，复活插件 registerToolAgent 死注册；creator 双份 prompt 合并（配置卡权威）。仍待跟进：ToolAgent 输出的统一评估/降级框架（spec 明确 anti-overengineering，暂不做 GUI 面板）。

---

## P1：GUI 未接入能力清理

前端中存在一些“组件已写、主流程未完全接入”的区域，需要二选一：接入或删除。

2026-06-11 已完成：个人资料设置、聊天头像/气泡主题化、默认樱花薄荷主题、工作台主题保留，以及 Agent 详情能力/任务/成长 tabs 的视觉整理已接入 GUI。仍需清理的是下列真实数据流或高级组件主流程。

**2026-08-04 全部完成**（接入或删除，不留中间态）：

- 扩展页技能执行按钮 → 已接入真实链路：SkillsTab 任务输入 → `execute_skill` → `skill_result` 结果展示 + `ws-error` 失败展示（此前按钮无 onClick，命令从未被发送）。
- WakeQueueSection → 已挂载到设置页「运维」组（修复 ws client 从未注入的断链，改用 `getWsClient()`）。
- GroupHealthPanel → 已挂载到群组详情「健康」tab。
- AgentTimeline → 已挂载到 Agent 详情「时间线」tab。
- 沙箱监控 CPU/内存/磁盘指标 → 后端 `docker stats --no-stream` 真实采集（`ContainerPool.stats()` + `parseDockerStats`），指标不可用时前端展示「—」而非假数据。
- 通知音效开关 → 已真实化：`lib/notify.ts` Web Audio 柔和提示音 + Tauri 系统通知，仅目标会话非当前查看或窗口失焦时触发。
- 任务回执卡片状态流转 → 已修复：同 butlerTaskId 事件改为更新已渲染卡片（`chat store updateTaskReceipt`），不再丢弃；群组派发回执已在群组会话展示。

---

## P1：管家入口产品化

**2026-08-04 全部完成**（研究 → 四阶段实施，见 `docs/superpowers/specs/2026-08-04-butler-entry-productization-research.md`；验证 63 files / 565 tests + 管家冒烟 19/19）。

- 首次使用（对话式）✅：2026-08-05 从问卷弹窗改为管家对话式收集（删除 OnboardingOverlay；首启欢迎消息引导 + JOB.md「首启对话」范式：收集兴趣/称呼/相处方式 → 按喜好切人格/记录称呼 → 创建 1-2 个初始 Agent → 轻量推荐 ≤1 次；新增 butler-list-personas/set-persona/update-style 工具）。
- 管家模板 ✅：`templates/butler/personas/` 4 人格（亲密朋友/专业秘书/学习陪伴/家庭助理）×（CHARACTER + JOB）；`butler_set_persona` 切换复制；GUI 管家设置新增「管家形象」区（称呼/欢迎语/模板）。
- 管家自我风格优化 ✅：`butler_update_style`（apply=true 写 CHARACTER.md 用户偏好段 + config.json name）。
- 任务转接体验 ✅：`butler_task_updated` 结构化广播（ButlerTaskReceiptPayload）→ 前端回执卡片首次真实点亮 + butlerTasks store 复活（GlobalTodoPanel 小计）+ ChatInputActions 派发菜单结构化 dispatch_task（支持 agent/group）+ 管家 JOB.md 分级转接规则（自己答/派发/先澄清/先问用户）。
- 多轮交互闭环 ✅：2026-08-05 管家 JOB.md「多步任务推进流程」（澄清→推进→确认点→继续：方案出稿/主观偏好/群组需决策必须停下等用户，任务置 waiting_user）；群组内 @用户（@用户/@主人/@老板/@user）唤醒用户进群组回复（路线 B），或经管家收束确认（路线 A）；群组 agent 平时消息不通知不计未读（低打扰）。
- 低打扰提示 ✅：Market 推荐纪律写入 4 份 JOB.md（官方且明显优于本地才轻量提示每会话 ≤1 次、社区必须确认、本地已有能力时闭嘴）。
- 地基修复 ✅：ensureButlerDir 创建管家文件体系；管家从固定 prompt 改为文件 prompt（EXPERIENCE.md/记忆真实进入 prompt，实时生效）。

仍待跟进（新出现的后续项）：管家人格切换后的言行变化需真实对话回归（回执卡片状态流转刷新与群组派发回执已于 2026-08-04 GUI 清理轮完成）；对话式首启的收集效果（初始 Agent 贴合度、用户对管家喜好的理解）需真实用户验证。

2026-08-04 真实测试新增待办：
- 沙箱镜像构建：代码修复已就位（构建路径 `resolveSandboxDir` + base→python→full 依赖链 + 构建命令 600s 超时 + Dockerfile.base useradd 去固定 UID），但本机 Docker Hub 网络不稳定（`Head registry-1.docker.io ... EOF`）导致镜像最终未建成。**建议 Docker Desktop 配置国内 registry mirror（如 `https://docker.m.daocloud.io`）后重跑** `scripts/build-sandbox.sh`，群组挂载（Agent 绑定群组工作区容器）即可完整生效。
- LLM Provider 网络不稳定（DeepSeek API 偶发 `fetch failed` / `terminated`）会中断群组成员的长产出轮次，demo HTML 在 15 分钟观察窗口内未完成；协作机制本身已全部验证通过（审核管道/wake 调度/工作区写入/记忆落盘），网络稳定后重跑 `npx tsx scripts/real-test-pvz.ts` 即可复验完整 demo 产出。

---

## P1：管家入口产品化

战略上管家应当承担三项职责：

- 和用户对话
- 管理智能体
- 管理群组

为了面向普通用户，需要继续增强：

- 首次使用对话式收集：管家在对话中询问用户兴趣、生活需求、工作习惯、偏好风格（2026-08-05 已替代问卷落地，收集效果待真实用户验证）。
- 管家模板：亲密朋友、专业秘书、学习陪伴、家庭助理等。
- 管家自我风格优化：在用户授权范围内调整语气、称呼、主动性。
- 任务转接体验：让用户清楚知道“管家何时自己答、何时转给 Agent、何时创建 Group”。
- 低打扰提示：只有官方认证资源明显优于本地创建时，才提示安装。

---

## P1：Market 分级机制落地

**2026-08-03 已完成 v1**（`packages/core/src/market/` + 扩展页 Market tab + 5 个 WS 命令 + Butler 推荐/安装工具 + 3 官方 1 社区内置示例资源；验证：61 files / 546 tests 全绿 + WS 冒烟 25/25）。

已实现的产品规则：

- 官方认证资源：可由管家优先推荐，提示应轻量。✅（butler-market-recommend 官方/认证优先）
- 未认证社区资源：必须向用户申请，并要求用户主动审查。✅（安装器门禁 + GUI 确认流双层）
- 私人生成资源：默认路径，由管家或系统本地创建。✅（market_list local 层聚合展示）
- Group > Agent > Skill 的依赖树需要在安装前展示清楚。✅（market_get/安装结果返回完整依赖树，GUI 递归渲染）
- 安装 Group 时要展示会连带安装哪些 Agents。✅
- 安装 Agent 时要展示会连带安装哪些 Skills/Plugins。✅（Skills 已支持；Plugins 未纳入安装链路）

仍待跟进（v1 边界之外的后续）：

- 官方认证审核流程定义（certified 层是预留目录，尚无审核入口）。
- 认证远程市场仓库（当前只支持本地 data/market/ 与随应用打包的 bundled/）。
- persona / plugin 资源类型进入 Market 安装链路。
- 社区资源版本更新与安全修复机制（fork 后更新、版本回滚）。

---

## P2：测试与验证体系更新

当前源码中约有 66 个 `.test.ts` 文件（vitest include 口径：packages 61 + gui-v2 5）。历史进度中出现过 403、417 等测试数量，不应再写死为当前事实。

后续应做：

- 使用 `pnpm test` 的实时输出作为测试数量来源。
- 为插件、MCP、GUI WebSocket、Butler 创建 Agent、Group 唤醒增加端到端测试。
- 前端缺少系统化测试，需要明确是否引入。
- 文档更新时记录是否运行测试；文档-only 变更可以不跑完整 build/test，但不能声称测试通过。

---

## P2：普通用户场景闭环

“辅助普通人成为超级个体”需要被拆成可验证场景。

建议优先做：

- 旅行规划：偏好收集、行程生成、预算、提醒、资料归档。
- 学习陪伴：目标拆解、每日计划、复盘、错题/知识点记忆。
- 家庭事务：账单提醒、采购清单、证件/预约提醒。
- 内容创作：选题、资料、草稿、审稿、发布清单。
- 个人研究：资料抓取、摘要、交叉验证、长期笔记。

每个场景都应验证：管家能不能低摩擦接住需求，是否需要 Agent/Group，是否真的节省用户精力。

---

## P2：管家工具分级（结构性约束"应派发"为"不能直接调"）

陈默专项真实验证（2026-08-08）发现：管家"工作类任务一律派发"靠 JOB.md 纪律（软约束），同一份 JOB.md 三次执行出现一次"管家亲自整理"（LLM 方差）。host 从不亲自干活是因为运行时移除执行工具（结构约束）。待办：

- 评估管家工具白名单：移除执行类工具（bash/edit-file 等）或做分级（协调+个人事务 md 工具 vs 执行工具）
- 管家保留：读日程/写 md、派发/收束/协调、Market 推荐、persona 管理
- 验证：重复陈默场景，确认管家"应派发"行为 100% 稳定

**2026-08-09 完成**：`BUTLER_DEFAULT_TOOLS` 移除 bash/edit-file/glob/grep（保留 read-file/write-file 供个人事务 md）；`runtime.createButler` 运行时强制过滤 + 修复持久化 config.json（对齐 host 硬约束）；4 人格 + 运行时 JOB.md 说明执行工具已被移除。冒烟 19/19 验证管家链路无破坏。陈默场景完整复验留待真实场景跑通。

---

## P2：core 运行时健壮性（OOM 防护产品化）

陈默专项发现 core 4GB 堆耗尽崩溃（2026-08-08 已修复根因：workingDir 静默兜底 project cwd + grep 无扫描上限）。后续产品化：

- start-core.ts 提高 `--max-old-space-size`（或配置化）
- 观察长会话内存增长，确认无泄漏
- 群组/会话上下文体积监控与告警

**2026-08-09 完成（部分）**：`start-core.ts` 堆上限自检（`COBEING_HEAP_MB` 默认 4096，低于建议 warn 提示 NODE_OPTIONS）；root package.json 加 `start:core`（`node --max-old-space-size=4096 --import tsx scripts/start-core.ts`）。长会话内存增长观察与上下文体积监控留待运行时长期观测。
