# P1.2 管家多步推理 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 增强管家多步推理能力：自主编排复杂任务、对话中主动建议、按需检查群组进展、感知 Agent 负载。

**Architecture:** System Prompt 驱动 — 利用现有 ConversationLoop 多轮 tool calling，删除单次 LLM 的 butler-analyze-task，增强 butler-list 输出，新增 butler-check-group 工具。

**Tech Stack:** TypeScript, Node.js

**构建:** `pnpm build`（仅后端变更，前端无影响）

---

## 文件变动

| 操作 | 文件 | 说明 |
|------|------|------|
| Modify | `packages/core/src/agent/butler.ts` | 删除 makeAnalyzeTaskTool；增强 makeListTool；新增 makeCheckGroupTool；更新 ButlerAgent 构造函数 |
| Modify | `packages/core/src/runtime.ts` | tools 白名单：删除 butler-analyze-task，新增 butler-check-group |

---

### Task 0: 增强 butler System Prompt（多步推理 + 主动建议）

**Files:** Modify: `packages/core/src/runtime.ts:127`

- [ ] **Step 1: 在默认 systemPrompt 中加入多步推理指南**

At line 127, replace the `||` fallback string. Add multi-step reasoning + proactive suggestion guidance after the existing group creation rules:

```typescript
      systemPrompt: butlerSelfConfig.systemPrompt || "你是管家，用户的第一联系人。像朋友一样跟用户聊天、帮忙、解决问题。\n\n创建群组时的规则：\n1. 先用 butler-list 查看已有的 Agent\n2. 如果已有 Agent 能胜任，直接用 butler-add-to-group 加入群组，不要重复创建\n3. 只有确实没有合适 Agent 时才用 butler-create-agent 创建新的\n4. Agent 按技能领域命名（如\"前端工程师\"），不按项目命名（如\"挂机游戏前端工程师\"）\n5. 同一个 Agent 可以同时属于多个群组\n\n## 多步推理能力\n当用户提出复杂任务（组建团队、项目开发等）时，你必须自主完成多步推理，在一次回复中连续调用多个工具直到任务完成。不要只返回分析文本然后等用户指示——直接调用工具执行。\n\n标准流程：\n1. butler-list → 了解已有 Agent 和群组\n2. 判断是否需要新建 Agent（复用优先）\n3. 如需新建 → butler-create-agent\n4. butler-create-group → 组建群组\n5. butler-run-group → 启动协作\n\n## 主动建议\n完成复杂任务后，在回复末尾评估是否需要向用户建议补充角色。如果群组缺少关键角色（如只有前端没有后端），自然地说\"我注意到当前团队还缺XX角色，需要我创建一个吗？\"",
```

- [ ] **Step 2: 验证编译**

```bash
cd D:\agent-codes\CoBeing && pnpm build
```

Expected: 编译通过。

---

### Task 1: 删除 butler-analyze-task 工具

**Files:** Modify: `packages/core/src/agent/butler.ts:600-653` (makeAnalyzeTaskTool), `:783` (registration)

- [ ] **Step 1: 删除 makeAnalyzeTaskTool 函数**

Delete lines 600-653 (the entire `function makeAnalyzeTaskTool(...)` block).

- [ ] **Step 2: 删除注册调用**

At line 783, delete:
```typescript
    this.toolRegistry.register(makeAnalyzeTaskTool(() => provider, this.butlerRegistry));
```

- [ ] **Step 3: 验证编译**

```bash
cd D:\agent-codes\CoBeing && pnpm build
```

Expected: packages/core 编译通过（可能有未使用 import 的 warning，后续步骤消除）。

---

### Task 2: 增强 butler-list 输出（Agent 负载感知）

**Files:** Modify: `packages/core/src/agent/butler.ts:371-385` (makeListTool)

- [ ] **Step 1: 增强 makeListTool 的 execute 方法**

Replace the execute function body (lines 376-382):

```typescript
    async execute(_params, _context: ToolContext): Promise<ToolResult> {
      const agents = registry.list().map(a => {
        const status = a.getStatus();
        const statusLabel = status === "running" ? "忙碌中" : status === "error" ? "异常" : "空闲";
        return `  - ${a.name} (${a.id}) [${statusLabel}]`;
      }).join("\n");
      const groups = groupManager.list().map(g => {
        const memberCount = g.config.members.length;
        return `  - ${g.config.name} (${g.id}) [${memberCount} 成员]`;
      }).join("\n");
      return {
        toolCallId: "",
        content: `## Agent 列表\n${agents || "  (无)"}\n\n## 群组列表\n${groups || "  (无)"}`,
      };
    },
```

Status mapping: `idle` → "空闲", `running` → "忙碌中", `error` → "异常".

- [ ] **Step 2: 验证编译**

```bash
cd D:\agent-codes\CoBeing && pnpm build
```

---

### Task 3: 新增 butler-check-group 工具

**Files:** Modify: `packages/core/src/agent/butler.ts` (add function + registration)

- [ ] **Step 1: 添加 makeCheckGroupTool 函数**

Add before the `// ---- butler-modify-agent ----` comment (before line 692). Insert:

```typescript
// ---- butler-check-group ----

function makeCheckGroupTool(groupManager: GroupManager): Tool {
  return {
    name: "butler-check-group",
    description: "检查群组进展：读取 PROGRESS.md 和 TODO 状态，返回结构化报告。当用户问\"进展如何\"时调用。",
    parameters: {
      type: "object",
      properties: {
        groupId: { type: "string", description: "群组 ID" },
      },
      required: ["groupId"],
    },
    async execute(params, _context: ToolContext): Promise<ToolResult> {
      const groupId = params.groupId as string;
      const group = groupManager.get(groupId);
      if (!group) {
        return { toolCallId: "", content: `未找到群组: ${groupId}`, isError: true };
      }

      const parts: string[] = [];
      parts.push(`## 群组进展报告: ${group.config.name}`);

      // 1. PROGRESS.md
      const progressContent = group.workspace.readProgress?.() ?? "";
      if (progressContent) {
        const preview = progressContent.slice(0, 1500);
        parts.push(`### PROGRESS.md\n${preview}${progressContent.length > 1500 ? "\n...(已截断)" : ""}`);
      } else {
        parts.push("### PROGRESS.md\n暂无进展记录");
      }

      // 2. TODO 统计
      const scanner = (groupManager as any).getScanner?.(groupId);
      if (scanner) {
        const store = scanner.getStore();
        const pending = store.list("pending");
        const completed = store.list("completed");
        const inProgress = store.list("in_progress");
        parts.push(`### TODO 状态`);
        parts.push(`- 待处理: ${pending.length} 项`);
        if (inProgress.length > 0) parts.push(`- 进行中: ${inProgress.length} 项`);
        parts.push(`- 已完成: ${completed.length} 项`);
        const total = pending.length + inProgress.length + completed.length;
        if (total > 0) {
          const rate = Math.round((completed.length / total) * 100);
          parts.push(`- 完成率: ${rate}%`);
        }
        if (pending.length > 0) {
          parts.push(`\n待处理任务:`);
          for (const t of pending.slice(0, 10)) {
            parts.push(`  - [${t.id}] ${t.title}`);
          }
          if (pending.length > 10) parts.push(`  ... 还有 ${pending.length - 10} 项`);
        }
      } else {
        parts.push("### TODO 状态\n无法获取 TODO 信息");
      }

      // 3. 成员活跃度
      const memberProfiles = group.getMemberProfiles?.() ?? [];
      if (memberProfiles.length > 0) {
        parts.push(`### 成员 (${memberProfiles.length})`);
        for (const m of memberProfiles) {
          parts.push(`  - ${m.name || m.id}`);
        }
      }

      return { toolCallId: "", content: parts.join("\n\n") };
    },
  };
}
```

- [ ] **Step 2: 在 ButlerAgent 构造函数中注册新工具**

After the `makeModifyAgentTool` registration line (after line 784), add:

```typescript
    this.toolRegistry.register(makeCheckGroupTool(groupManager));
```

- [ ] **Step 3: 验证编译**

```bash
cd D:\agent-codes\CoBeing && pnpm build
```

---

### Task 4: 更新 runtime.ts 工具白名单

**Files:** Modify: `packages/core/src/runtime.ts:132-138`

- [ ] **Step 1: 替换 tools 白名单中的 butler-analyze-task**

Find the line containing `"butler-analyze-task"` and replace with `"butler-check-group"`:

```typescript
        "butler-read-registry", "butler-update-registry", "butler-check-group", "butler-modify-agent",
```

- [ ] **Step 2: 验证编译 + 测试**

```bash
cd D:\agent-codes\CoBeing && pnpm build && pnpm test
```

Expected: 6 packages build pass, 281+ tests pass.

---

### Task 5: 更新文档

**Files:** Modify: `PROGRESS.md`, `docs/待办新.md`

- [ ] **Step 1: 更新 PROGRESS.md**

在顶部追加 P1.2 完成条目。

- [ ] **Step 2: 更新 待办新.md**

将 1.2 节 4 项标记为 ✅。

- [ ] **Step 3: 更新 STRUCTURE.md 和 后端能力清单.md**（如需要）

butler-check-group 是新增工具，需在后端能力清单的 ButlerAgent 部分添加说明。
