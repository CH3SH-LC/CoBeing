# P1.3 群组生命周期管理 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为群组添加完整生命周期：状态机（active→completed→archived）、自动完成检测、硬归档/恢复、工作区文档自动同步。

**Architecture:** GroupConfig 新增 status 字段 → Group.postMessage 加冻结检查 → GroupManager 新方法 → GroupTodoScanner 完成检测 + 文档同步 → WS 广播通知前端。

**Tech Stack:** TypeScript, better-sqlite3, ws, React 19, Tailwind CSS

**构建:** `pnpm build`（后端）+ `cd gui-v2 && npm run build`（前端）

---

## 文件变动

| 操作 | 文件 | 说明 |
|------|------|------|
| Modify | `packages/shared/src/types.ts` | GroupConfig + GroupStatusInfo 加 status |
| Modify | `packages/core/src/group/group.ts` | postMessage 状态检查 + setStatus |
| Modify | `packages/core/src/group/manager.ts` | completeGroup / archiveGroup / restoreGroup |
| Modify | `packages/core/src/todo/group-scanner.ts` | 完成检测 + 文档同步 |
| Modify | `packages/core/src/api/ws-server.ts` | group_updated 事件广播 |
| Modify | `gui-v2/src/lib/types.ts` | GroupStatusInfo 加 status |
| Modify | `gui-v2/src/components/layout/Sidebar.tsx` | 渲染群组状态标签 |

---

### Task 1: GroupConfig 类型增强

**Files:** Modify: `packages/shared/src/types.ts:292-298` (GroupConfig), `:316-321` (GroupStatusInfo)

- [ ] **Step 1: GroupConfig 加 status 字段**

Edit `packages/shared/src/types.ts`, add `status` to `GroupConfig`:

```typescript
export interface GroupConfig {
  id: string;
  name: string;
  members: string[];
  owner?: string;
  topic?: string;
  status?: 'active' | 'completed' | 'archived';
}
```

- [ ] **Step 2: GroupStatusInfo 加 status 字段**

Edit same file, add `status` to `GroupStatusInfo`:

```typescript
export interface GroupStatusInfo {
  id: string;
  name: string;
  members: string[];
  messageCount: number;
  status?: 'active' | 'completed' | 'archived';
}
```

- [ ] **Step 3: 验证编译**

```bash
cd D:\agent-codes\CoBeing && pnpm build
```

Expected: 所有 packages 编译通过。

---

### Task 2: Group 状态机 + 冻结规则

**Files:** Modify: `packages/core/src/group/group.ts`

- [ ] **Step 1: 构造函数初始化 status**

In Group constructor (after `this.config = config;`), add:

```typescript
    if (!this.config.status) (this.config as any).status = 'active';
```

- [ ] **Step 2: postMessage 添加状态检查**

At the beginning of `postMessage()` (before `const msg = this.ctxV2.append(...)`), add:

```typescript
    if (this.config.status === 'archived') {
      throw new Error(`群组 ${this.config.name} 已归档，无法发送消息`);
    }
    if (this.config.status === 'completed') {
      throw new Error(`群组 ${this.config.name} 已完成，无法发送消息。如需继续请先恢复为活跃状态`);
    }
```

- [ ] **Step 3: 添加 setStatus 方法**

Add method to Group class:

```typescript
  setStatus(status: 'active' | 'completed' | 'archived'): void {
    (this.config as any).status = status;
  }
```

- [ ] **Step 4: 验证编译**

```bash
cd D:\agent-codes\CoBeing && pnpm build
```

---

### Task 3: GroupManager 生命周期方法

**Files:** Modify: `packages/core/src/group/manager.ts`

- [ ] **Step 1: 添加 completeGroup 方法**

Add after existing methods:

```typescript
  completeGroup(groupId: string): boolean {
    const group = this.groups.get(groupId);
    if (!group) return false;
    group.setStatus('completed');
    group.workspace.appendProgress('System', `群组自动标记为已完成 — 所有 TODO 已关闭`);
    this.saveGroupConfig(groupId);
    log.info("Group completed: %s", groupId);
    return true;
  }
```

- [ ] **Step 2: 添加 archiveGroup 方法**

```typescript
  archiveGroup(groupId: string): boolean {
    const group = this.groups.get(groupId);
    if (!group || group.config.status === 'archived') return false;

    group.setStatus('archived');
    group.dispose();
    this.groupScanners.get(groupId)?.stop();
    this.groupScanners.delete(groupId);
    this.groups.delete(groupId);

    // Pack to zip
    const groupDir = path.join(this.groupsDir, groupId);
    const archiveDir = path.join(this.dataRoot, 'archives');
    fs.mkdirSync(archiveDir, { recursive: true });
    const archivePath = path.join(archiveDir, `${groupId}.zip`);

    try {
      const { execSync } = require('node:child_process');
      execSync(`powershell -Command "Compress-Archive -Path '${groupDir}' -DestinationPath '${archivePath}' -Force"`, { stdio: 'ignore' });
      rmDirRecursive(groupDir);
      log.info("Group archived: %s → %s", groupId, archivePath);
    } catch (err: any) {
      log.error("Failed to archive group %s: %s", groupId, err.message);
      return false;
    }
    return true;
  }
```

- [ ] **Step 3: 添加 restoreGroup 方法**

```typescript
  restoreGroup(groupId: string): Group | undefined {
    const archivePath = path.join(this.dataRoot, 'archives', `${groupId}.zip`);
    if (!fs.existsSync(archivePath)) {
      log.warn("Archive not found for group: %s", groupId);
      return undefined;
    }

    const groupDir = path.join(this.groupsDir, groupId);
    try {
      const { execSync } = require('node:child_process');
      fs.mkdirSync(groupDir, { recursive: true });
      execSync(`powershell -Command "Expand-Archive -Path '${archivePath}' -DestinationPath '${groupDir}' -Force"`, { stdio: 'ignore' });
      fs.unlinkSync(archivePath);
    } catch (err: any) {
      log.error("Failed to restore group %s: %s", groupId, err.message);
      return undefined;
    }

    // Re-load config
    const configPath = path.join(groupDir, 'config.json');
    if (!fs.existsSync(configPath)) {
      log.error("Group config not found after restore: %s", groupId);
      return undefined;
    }
    const config = JSON.parse(fs.readFileSync(configPath, 'utf-8'));
    config.status = 'active';

    const group = new Group(config, this.registry, this.dataRoot);
    group.setGroupManager(this);
    if (this._onAgentResponse) group.setOnAgentResponse(this._onAgentResponse);
    if (this._onAgentEvent) group.setOnAgentEvent(this._onAgentEvent);
    if (this._onQueueChange) group.setOnQueueChange(this._onQueueChange);
    this.groups.set(groupId, group);
    this.saveGroupConfig(groupId);

    // Restart scanner
    const scanner = new (require('../todo/group-scanner.js').GroupTodoScanner)(groupId, groupDir, {
      onTrigger: async (gid, todo, msg) => {
        const g = this.groups.get(gid);
        if (g) g.postMessage('TODOboard', msg);
      },
      onCompleteAction: async (gid, todo) => {
        const g = this.groups.get(gid);
        if (g && todo.onComplete?.mentionAgentId) {
          g.postMessage('system', todo.onComplete.message || `@${todo.onComplete.mentionAgentId} ${todo.title} 已完成`);
        }
      },
      onDependencyMet: async (gid, todo) => {
        const g = this.groups.get(gid);
        if (g && todo.targetAgentId) {
          g.postMessage('TODOboard', `@${todo.targetAgentId} 【依赖完成通知】上游任务已完成`);
        }
      },
    });
    scanner.start();
    this.groupScanners.set(groupId, scanner);
    log.info("Group restored: %s", groupId);
    return group;
  }
```

Note: Add `import fs from "node:fs";` if not already present at top of manager.ts.

- [ ] **Step 4: 添加 saveGroupConfig helper**

```typescript
  private saveGroupConfig(groupId: string): void {
    const group = this.groups.get(groupId);
    if (!group) return;
    const groupDir = path.join(this.groupsDir, groupId);
    fs.mkdirSync(groupDir, { recursive: true });
    fs.writeFileSync(path.join(groupDir, 'config.json'), JSON.stringify(group.config, null, 2), 'utf-8');
  }
```

- [ ] **Step 5: 验证编译**

```bash
cd D:\agent-codes\CoBeing && pnpm build
```

---

### Task 4: GroupTodoScanner 完成检测 + 文档同步

**Files:** Modify: `packages/core/src/todo/group-scanner.ts`

- [ ] **Step 1: 在 scanOnce 末尾添加完成检测**

After the `Promise.allSettled` line in `scanOnce()`, add:

```typescript
    // Completion detection: all TODOs done → mark group completed
    try {
      const allTodos = this.store.list();
      if (allTodos.length > 0 && allTodos.every(t => t.status === 'completed')) {
        const gm = (globalThis as any).__cobeingGroupManager;
        if (gm) {
          const lastMsg = gm.get(this.groupId)?.groupDb?.getAllMessages({ limit: 1 });
          const lastMsgAge = lastMsg?.length ? Date.now() - lastMsg[0].timestamp : Infinity;
          if (lastMsgAge > 3600000) {
            gm.completeGroup?.(this.groupId);
          }
        }
      }
    } catch (err: any) {
      log.error("Completion check failed for %s: %s", this.groupId, err.message);
    }
```

- [ ] **Step 2: 在 complete 方法中添加文档同步**

At the end of the `complete()` method, after `this.callbacks.onCompleteAction`, add workspace sync:

```typescript
    // Auto-sync workspace: append to PROGRESS.md
    try {
      const gm = (globalThis as any).__cobeingGroupManager;
      if (gm) {
        const group = gm.get(this.groupId);
        if (group) {
          group.workspace.appendProgress('System', `TODO "${item.title}" 已完成`);
        }
      }
    } catch (err: any) {
      log.error("Workspace sync failed for %s: %s", this.groupId, err.message);
    }
```

- [ ] **Step 3: 验证编译**

```bash
cd D:\agent-codes\CoBeing && pnpm build
```

---

### Task 5: WS 事件 + 前端适配

**Files:** Modify: `ws-server.ts`, `gui-v2/src/lib/types.ts`, `Sidebar.tsx`

- [ ] **Step 1: ws-server.ts — completeGroup 后广播**

In `manager.ts` `completeGroup()` method, after `group.workspace.appendProgress(...)`, add:

```typescript
    // Broadcast to frontend
    const ws = (globalThis as any).__cobeingWSServer;
    if (ws) {
      ws.broadcast({ type: 'group_updated', payload: { id: groupId, name: group.config.name, status: 'completed' } });
    }
```

Also add `(globalThis as any).__cobeingWSServer = this;` in ws-server.ts constructor (`CoreWSServer` class, around line 67-68).

- [ ] **Step 2: 前端 types.ts — GroupStatusInfo 加 status**

In `gui-v2/src/lib/types.ts`, find `GroupStatusInfo` and add:

```typescript
export interface GroupStatusInfo {
  id: string;
  name: string;
  members: string[];
  messageCount: number;
  status?: 'active' | 'completed' | 'archived';
}
```

- [ ] **Step 3: 前端 Sidebar.tsx — 渲染状态标签**

In the group rendering section (around line 139), after the group name, add status badge:

```typescript
                <div className="text-sm text-purple font-medium truncate">{group.name}</div>
                {group.status === 'completed' && (
                  <span className="text-[10px] px-1.5 py-0.5 rounded bg-green/10 text-green">已完成</span>
                )}
                {group.status === 'archived' && (
                  <span className="text-[10px] px-1.5 py-0.5 rounded bg-txt-muted/10 text-txt-muted">已归档</span>
                )}
```

- [ ] **Step 4: 验证编译**

```bash
cd D:\agent-codes\CoBeing && pnpm build && cd gui-v2 && npm run build
```

---

### Task 6: 测试 + 文档更新

- [ ] **Step 1: 运行全部测试**

```bash
cd D:\agent-codes\CoBeing && pnpm test
```

Expected: 281+ tests pass.

- [ ] **Step 2: 更新 PROGRESS.md + docs/待办新.md**

Mark P1.3 items as complete.
