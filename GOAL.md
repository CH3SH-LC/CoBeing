# CoBeing 项目愿景

> 最后更新：2026-06-07

---

## 一句话定位

CoBeing 要成为面向普通人的个人 AI Agent Team 系统：一个可个性化的管家入口，管理多个专门智能体和工作群组，帮助用户把生活、学习、创作、研究和事务处理中的一部分杂事交给 AI 团队完成。

更进一步，它希望辅助普通人成为“超级个体”：不是把用户变成企业管理者，而是让每个人都能拥有自己的小型智能体团队。

---

## 与 To B 产品的区别

CoBeing 不以企业组织结构为中心，也不把目标设定为公司管理、部门协同或流程整合工具。

它面向的是个人用户，尤其是下沉市场和非技术用户。产品应该解决的是“我的生活里有很多琐碎、重复、需要长期记忆和个人风格的事情”，而不是“公司需要把员工和系统整合到统一工作台”。

因此，CoBeing 的默认体验应该像微信式的对话入口，而不是企业后台、低代码平台或流程管理软件。

---

## 三个发展方向

### 1. 模块化与插件化

用户需求高度个性化，不可能靠一个固定应用覆盖所有人。因此系统需要把能力拆成可组合资源：

- Skill：工作方法或专门流程
- Plugin：Provider、Channel、UI、工具等扩展能力
- Agent：有角色、职责、工具和记忆的工作个体
- Group：多个 Agent 组成的协作结构
- Butler Persona：管家的形象、语气和关系模板

模块化的目标不是让普通用户自己研究插件，而是让系统能够按需生成、安装、替换和审查扩展。

### 2. 平台化入口

传统入口是浏览器和 App。CoBeing 想探索的新入口是“智能体对话与协作空间”。

用户不是打开一个个 App，再学习每个 App 的功能，而是对管家说出需求，由管家判断：

- 直接回答
- 调用一个 Agent
- 创建一个新 Agent
- 转交给一个 Group
- 推荐官方认证扩展
- 请求用户授权安装社区扩展

这要求管家成为默认入口，Agent 和 Group 成为任务承载层，Market 成为后台扩展枢纽。

### 3. 多智能体协作能力增强

CoBeing 的核心不是单个聊天机器人，而是 Agent Team。

协作能力需要同时满足两件事：

- 规范：任务拆解、角色分工、上下文隔离、进度同步、结果验收、经验沉淀。
- 个性化：每个 Agent 和管家都可以有自己的风格、判断标准、工作偏好和长期关系。

过度规范会变成企业流程工具；过度自由会变成混乱聊天。CoBeing 的难点就是在中间找到个人用户可用的协作范式。

---

## 产品表层：管家

管家不应该承担所有具体工作。它的职责应收敛为三件事：

1. 和用户对话。
2. 管理智能体。
3. 管理群组。

这样，管家可以把优化重点放在对话、关系、风格、理解用户和调度资源上。具体工作交给专门 Agent 或 Group 完成。

管家的角色应由用户决定。系统可以提供模板，例如：

- 亲密朋友
- 专业秘书
- 学习陪伴
- 家庭助理
- 创作搭档

用户应有较高权限修改管家的风格。管家也可以在用户授权下，依据长期互动逐步优化称呼、语气、主动性和工作习惯。

---

## Market 与扩展策略

Market 不应该默认暴露给普通用户。

普通用户直接浏览 market 会遇到三个问题：

- 他们不知道自己需要 Skill、Plugin、Agent 还是 Group。
- 他们很难判断社区扩展质量和安全风险。
- 每次工作中弹出大量候选项会增加认知负担。

因此，Market 应该是扩展枢纽，而不是普通用户的主要界面。

### 分级规则

- 官方认证资源：通过可靠审核，可以由管家优先推荐。
- 社区未认证资源：必须向用户申请，并要求用户主动审查后授权。
- 本地自主创建资源：默认路径，由管家或系统根据用户需求生成。

### 资源层级

- Group 最大：安装 Group 往往需要安装结构和相关 Agents。
- Agent 次之：安装 Agent 可能需要安装相关 Skills 和 Plugins。
- Skill/Plugin 最小：提供局部能力、方法或接口扩展。

### 推荐原则

默认应本地创建。只有当官方认证 market 中存在明显更优候选时，管家才轻量提示。

未认证社区资源不能由管家静默安装。它必须把来源、权限、依赖、风险和替代方案告诉用户。

---

## 初次使用体验

更适合普通用户的初次体验不是裸应用，也不是一次性塞满大量 Agent。

理想路径类似兴趣问卷：

1. 用户首次打开产品。
2. 管家询问用户生活需求、兴趣、工作/学习目标、语气偏好。
3. 系统根据结果创建少量初始 Agent。
4. 如果官方认证资源明显匹配，再轻量推荐安装。
5. 用户之后主要通过管家继续增删改 Agent 和 Group。

这样既避免裸应用无从下手，也避免预装太多无关资源造成噪音。

---

## 当前代码事实

当前项目已经具备：

- CoBeingRuntime 后端编排器。
- Butler、Agent、Group 三层主体。
- React/Tauri GUI 六入口。
- Agent 文件体系：`AGENTS.md`、`CHARACTER.md`、`JOB.md`、`MEMORY.md`、`EXPERIENCE.md`、`config.json`。
- 工具、权限、沙箱、记忆、TODO、技能、插件、MCP 基础设施。
- 默认 DeepSeek Provider，以及通过数据插件扩展其他 Provider 的基础。

当前仍需建设：

- Market 官方认证/社区分级体验。
- 管家自动推荐扩展的规则。
- 插件、MCP、投票、协作验收等端到端验证。
- 普通用户初次使用引导。
- 生活场景闭环，证明系统真的能减少用户杂事。

---

## 判断产品价值的标准

CoBeing 是否值得用户选择，不应只看“能不能聊天”。

它应该在这些问题上胜出：

- 用户是否能少打开几个 App？
- 用户是否能把重复事务交给长期记忆的 Agent？
- 用户是否能用自己的风格塑造管家和智能体？
- 用户是否能在复杂任务中自然获得一个小团队，而不是自己拆解流程？
- 用户是否能信任系统安装和调用扩展？
- 用户是否能逐步积累个人工作方式，而不是每次从零开始？

如果这些问题回答不好，CoBeing 就会退化成“又一个聊天壳”。如果回答得好，它才可能成为个人 AI 入口。

