{
  "name": "cobeing",
  "version": "1.4.0",
  "private": true,
  "description": "Multi-agent architecture with multi-LLM, multi-channel, sandbox, MCP, skills and native GUI",
  "type": "module",
  "scripts": {
    "dev": "tsx scripts/dev.ts",
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "clean": "pnpm -r run clean",
    "start:core": "node --max-old-space-size=4096 --import tsx scripts/start-core.ts"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "js-yaml": "^4.1.1",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "ws": "^8.20.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "node-llama-cpp": "^3.18.1"
  }
}
